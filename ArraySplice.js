// Splice method changes original array, so it mutates that array.


let array1 = ['Sunday','Monday','Tuesday','Wednesday','Thursday','Friday','Saturday'];

console.log(array1.splice(2));      //      here original array has been updated.

console.log(array1);        //  As per spilce method all the elements were deleted by spilce methods and only two elements are left in array.

